from odoo import models, fields

class Stagiaire(models.Model):
    _name = 'stagiaire'
    # Champ Many2one pour lier chaque stagiaire à un encadreur


    #id_stagiaire = fields.Integer('ID stagiaire', required=True)
    #nom_stagiaire = fields.Char(string='Nom', required=True)
    #email = fields.Char(string='email', required=True)
   # telephone = fields.Integer('telephone', required=True)
   # institut = fields.Char(string='institut', required=True)

